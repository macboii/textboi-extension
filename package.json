{
  "name": "textboi-extension",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "clean": "rm -rf dist/",
    "pack": "node pack.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "diff-match-patch": "^1.0.5",
    "franc": "^6.2.0",
    "gpt-tokenizer": "^3.4.0"
  }
}
